package models;

public class DrawDeck extends Deck {
	
	public DrawDeck() {
		super();
	}
	
	public void shuffleDeck() {
		//Loop through the cards list and set the card at the current index to an random index between the current index + 1 and the list size - 1
		//Set the index of that card to the index of the first card
	}
	
	public Card removeCard() {
		//Remove the card from the last index of the list
		return null;
	}
}
